# 三源并发混合搜索 - 实施说明

## 实施日期
2026年1月13日

## 实施方案
**方案 B：增强 researcher_node**
- 保持原有 Graph 结构不变
- 在 researcher_node 内部拦截执行流程
- 并发调用 Web/RAG/ES 三个数据源
- 聚合结果并注入到上下文中

## 关键修改

### 1. 新建文件：`src/graph/hybrid_search.py`
**功能：** 并发多源搜索模块

**核心函数：**
- `parallel_search()` - 主函数，执行并发搜索
- `_search_web()` - Web 搜索（Tavily/DuckDuckGo/Serper等）
- `_search_rag()` - RAG 向量数据库搜索
- `_search_elasticsearch()` - Elasticsearch 全库搜索

**特性：**
- 使用 `asyncio.gather()` 实现真正的并发
- 即使没有提供 resources 参数，也会强制执行搜索
- 优雅处理异常，单个搜索失败不影响其他搜索
- 聚合所有结果为统一的上下文字符串

### 2. 修改文件：`src/graph/nodes_2.py`
**重要：** 系统实际使用的是 `nodes_2.py`，不是 `nodes.py`！

在 `researcher_node()` 函数中添加：

1. **混合搜索集成（第1344-1383行）**
   ```python
   from src.graph.hybrid_search import parallel_search
   
   # 提取当前步骤的查询
   query = f"{current_step.title} {current_step.description}"
   
   # 执行并发搜索
   hybrid_result = await parallel_search(
       query=query,
       resources=state.get("resources", []),
       enable_web=True,
       enable_rag=True,
       enable_es=True,
       max_web_results=configurable.max_search_results
   )
   ```

2. **上下文注入（第1385-1405行）**
   ```python
   # 将聚合的搜索结果注入到 state messages 中
   context_message = HumanMessage(
       content=f"""# Background Context from Multi-Source Search
       
User did not provide specific resources, but here is relevant context found from internal and external sources:

{hybrid_context}

Please use this context to inform your research...""",
       name="hybrid_search_context"
   )
   ```

## 备份文件
**位置：** `archive/backup_before_hybrid_v1/`

已备份：
- `nodes_2.py` ✓ （实际使用的文件）
- `nodes.py` ✓ （未使用，但已备份）
- `builder.py` ✓
- `search.py` ✓
- `retriever.py` ✓

## 数据源配置

### Web 搜索
- **引擎：** Tavily, DuckDuckGo, Serper, Brave, Arxiv 等
- **配置：** 通过 `conf.yaml` 中的 `SEARCH_ENGINE` 设置
- **结果数：** 通过 `max_search_results` 配置

### RAG 搜索
- **提供商：** RAGFlow, Dify, VikingDB 等
- **配置：** 通过环境变量和 `conf.yaml`
- **行为：** 搜索配置的知识库

### Elasticsearch 搜索
- **模式：** 统一全库搜索
- **环境变量：**
  - `ELASTICSEARCH_HOST`
  - `ELASTICSEARCH_USER`
  - `ELASTICSEARCH_PASSWORD`
  - `ELASTICSEARCH_TARGET_INDICES` (可选)
- **默认索引：**
  - `2_cn_news_library` (新闻库)
  - `2_cn_post_library` (帖子库)
  - `3_cn_targetmap_*` (实体库)
  - `2_cn_comment_library` (评论库)
  - `2_cn_forum_library` (论坛库)
  - `2_cn_interactive_library` (互动库)

## 测试

### 运行测试脚本
```bash
cd deer-flow_pre/deer-flow
python3 test_hybrid_search.py
```

### 验证实施
```bash
./verify_hybrid_implementation.sh
```

### 查看日志
启动应用后，在日志中搜索 `[Hybrid Search]` 标记：
```bash
tail -f logs/app.log | grep "Hybrid Search"
```

## 工作流程

1. **用户提交查询** → coordinator_node
2. **生成研究计划** → planner_node
3. **执行研究步骤** → researcher_node
4. **【新增】并发搜索** → parallel_search()
   - 同时调用 Web、RAG、ES
   - 聚合所有结果
5. **注入上下文** → 添加到 state messages
6. **Agent 执行** → 使用注入的上下文 + 可用工具
7. **生成报告** → reporter_node

## 关键特性

### ✅ 强制执行搜索
- 不再依赖 `@` 符号触发
- 即使用户没有指定资源，也会自动搜索
- 确保每个研究步骤都有充足的背景信息

### ✅ 真正的并发
- 使用 `asyncio.gather()` 并发执行
- 总耗时 ≈ 最慢的单个搜索
- 显著提升搜索效率

### ✅ 优雅降级
- 单个搜索失败不影响其他搜索
- 部分结果仍然可用
- 详细的错误日志

### ✅ 保持兼容性
- 不改变原有 Graph 结构
- Agent 仍可使用原有工具
- 向后兼容现有功能

## 回滚方法

如果遇到问题，恢复备份：

```bash
cd deer-flow_pre/deer-flow
cp archive/backup_before_hybrid_v1/nodes_2.py src/graph/
```

**重要提示：** 恢复的是 `nodes_2.py`，不是 `nodes.py`！

## 验证清单

- [x] 备份文件已创建
- [x] `hybrid_search.py` 已创建
- [x] `nodes_2.py` 已修改（不是 nodes.py）
- [x] Python 语法检查通过
- [x] 验证脚本全部通过（27/27）
- [x] 测试脚本已创建
- [x] 文档已更新

## 下一步

1. **运行测试：** `python3 test_hybrid_search.py`
2. **启动应用：** 测试实际查询
3. **监控日志：** 查看 `[Hybrid Search]` 消息
4. **性能调优：** 根据实际情况调整参数

## 常见问题

### Q: 为什么修改 nodes_2.py 而不是 nodes.py？
**A:** 因为 `builder.py` 导入的是 `nodes_2.py`：
```python
from .nodes_2 import (
    researcher_node,
    coordinator_node,
    ...
)
```

### Q: 如何禁用某个搜索源？
**A:** 在 `researcher_node` 中修改 `parallel_search()` 调用：
```python
hybrid_result = await parallel_search(
    query=query,
    enable_web=True,   # 启用 Web 搜索
    enable_rag=False,  # 禁用 RAG 搜索
    enable_es=True,    # 启用 ES 搜索
)
```

### Q: 如何调整搜索结果数量？
**A:** 通过 `conf.yaml` 中的 `max_search_results` 配置，或直接修改调用：
```python
hybrid_result = await parallel_search(
    query=query,
    max_web_results=10  # 增加到 10 个结果
)
```

### Q: 搜索太慢怎么办？
**A:** 
1. 检查网络连接
2. 减少 `max_web_results`
3. 禁用不需要的搜索源
4. 检查 Elasticsearch 连接

## 技术支持

遇到问题时：
1. 查看日志：`logs/app.log`
2. 运行验证脚本：`./verify_hybrid_implementation.sh`
3. 检查环境变量配置
4. 参考详细文档：`HYBRID_SEARCH_IMPLEMENTATION.md`
